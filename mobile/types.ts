/**
 * Enum for OpenAI voice options
 * These voices cannot be changed during a session once the model has responded with audio at least once.
 */
export enum Voice {
  ALLOY = "alloy",
  ASH = "ash",
  BALLAD = "ballad",
  CORAL = "coral",
  ECHO = "echo",
  SAGE = "sage",
  SHIMMER = "shimmer",
  VERSE = "verse",
}
